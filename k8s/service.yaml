apiVersion: v1
kind: Service
metadata:
  name: llm-inference
  namespace: infra-ml
  labels:
    app: llm-inference
spec:
  selector:
    app: llm-inference
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP